{
  "configurations": [
    {
      "type": "cppvsdbg",
      "request": "launch",
      "name": "Debug Rust Program",
      "program": "${workspaceFolder}/target/debug/program.exe",
      "preLaunchTask": "cargo build",

      "cwd": "${workspaceFolder}"
    }
  ]
}
